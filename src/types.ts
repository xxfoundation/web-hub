export type Product = {
  logo: string;
  title: string;
  description: string;
  url: string;
}

export type Category = {
  title: string;
}

export type Endpoint = {
  url: string;
  host: string;
  description: string;
}